define(['angular'], function(angular) {
    return angular.module('kApp', ['ngRequire','ngCookies','ui.router', 'ui.bootstrap'])
        .run(['$rootScope', function($rootScope) {
            
        }]);
});